-----------------------------------------------------------------------------------------
说明:
    多例模式:
    1、多例类接收不同的参数，返回与参数对应的对象
    2、相同的参数，返回的是同一个实例(单例)

    欢迎大家交流，共同进步
-----------------------------------------------------------------------------------------