-----------------------------------------------------------------------------------------
说明:
    简单工厂(静态工厂)模式:
    1、简单工厂类返回的是对象
    2、通过简单工厂类内部的静态方法返回不同的对象
    3、返回的这些对象有共同的一些方法
    4、在简单工厂内部增加新的对象时，就需要修改简单工厂类，不符合开闭原则

    欢迎大家交流，共同进步
-----------------------------------------------------------------------------------------